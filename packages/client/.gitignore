# Paraglide's compiled messages. Regenerated by this package's `prepare` script,
# which npm runs both on install and before packing — so it is never checked in,
# but it IS in the published tarball. See docs/adr/0016.
/src/lib/paraglide/
