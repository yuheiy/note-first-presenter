{
  "name": "note-first-presenter-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp run test:unit && vp run test:integration && vp run test:e2e",
    "test:unit": "vp run --filter './packages/*' test",
    "test:integration": "vp run note-first-presenter#build && vp test",
    "test:e2e": "vp run note-first-presenter#build && playwright test",
    "prepare": "vp config",
    "dev": "vp run demo#dev",
    "check": "vp check && knip"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "knip": "catalog:",
    "note-first-presenter": "workspace:*",
    "playwright": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.5.1"
}
