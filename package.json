{
  "name": "note-first-presenter-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run test:cli && vp run test:e2e && vp run -r build",
    "test:cli": "vp run -F note-first-presenter test:cli",
    "test:e2e": "playwright test",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "happy-dom": "catalog:",
    "playwright": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.5.1"
}
