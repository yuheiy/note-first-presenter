{
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon -w ../packages/note-first-presenter/dist -e mjs --exec note-first-presenter",
    "build": "note-first-presenter build",
    "export": "note-first-presenter export"
  },
  "dependencies": {
    "note-first-presenter": "workspace:"
  },
  "devDependencies": {
    "nodemon": "catalog:"
  }
}
